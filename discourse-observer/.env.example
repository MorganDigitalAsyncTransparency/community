# Discourse API connection
#
# Default: local mock server (no real Discourse instance needed).
# Replace with your forum URL and credentials for production use.
DISCOURSE_BASE_URL=http://localhost:9920
DISCOURSE_API_TOKEN=
DISCOURSE_API_USER=
